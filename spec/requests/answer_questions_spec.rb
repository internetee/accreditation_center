require 'rails_helper'
require_relative "../support/devise"

RSpec.describe "AnswerQuestions", type: :request do

  # let(:quiz) { build(:quiz, id: 1) }
	# let(:question) { build(:category, id: 1) }
  # let(:answer) { create(:answer, id: 1) }

  # before(:each) do
  #   # @request.env["devise.mapping"] = Devise.mappings[:superadmin]
  #   sign_in FactoryBot.create(:user)
  # end

  # describe "POST /create" do

  #   it "returns http success" do
      
  #     post "/answer_questions", params: { answer_question: {quiz_id: quiz.id, answer_id: answer.id , question_id: question.id } }

  #     expect(response).to have_http_status(:success)
  #   end
  # end

end
