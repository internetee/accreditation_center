require 'rails_helper'

RSpec.describe Ability, type: :model do
  # include Rack::Test::Methods

  # context 'check validation' do
	# 	let(:user) { FactoryBot.create(:user, superadmin_role: true) }

  #   it 'can create new quiz' do
	# 		@ability = Ability.new(user)
	# 		expect(@ability).to be_able_to(:manage, Question.new)
  #   end
  # end
end
